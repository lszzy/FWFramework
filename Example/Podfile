platform :ios, '9.0'
use_frameworks!

target "Example" do
  pod 'FWFramework', :path => '../'
  pod 'FWDebug', :configurations => ['Debug']
end

target "AppClip" do
  pod 'FWFramework', :path => '../', :subspecs => ['Image', 'Json', 'Request', 'SwiftUI']
  pod 'FWDebug', :configurations => ['Debug']
end
