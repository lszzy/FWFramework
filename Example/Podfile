platform :ios, '9.0'
use_frameworks!

target "Example" do
  
  pod 'FWFramework', :path => '../'
  pod 'FWDebug', :configurations => ['Debug']
end

target "AppClip" do
  pod 'FWFramework', :path => '../', :subspecs => ['Application/Image', 'Application/Json', 'Application/Request', 'Component/SwiftUI']
  pod 'FWDebug', :configurations => ['Debug']
end
